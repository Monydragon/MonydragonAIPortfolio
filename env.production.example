# Production Environment Variables
# Copy this to .env.local and fill in your actual values

# ============================================
# Database Configuration
# ============================================
# MongoDB URI - For Docker container (default)
MONGODB_URI=mongodb://localhost:27017/monydragon_portfolio

# For MongoDB with authentication (production):
# MONGODB_URI=mongodb://username:password@localhost:27017/monydragon_portfolio?authSource=admin

# For MongoDB Atlas (cloud):
# MONGODB_URI=mongodb+srv://username:password@cluster.mongodb.net/monydragon_portfolio?retryWrites=true&w=majority

# MongoDB Docker Configuration (optional, for docker-compose.yml)
MONGODB_PORT=27017
# MONGO_ROOT_USERNAME=admin
# MONGO_ROOT_PASSWORD=your-secure-password-here

# ============================================
# NextAuth Configuration (REQUIRED)
# ============================================
# Your production URL
NEXTAUTH_URL=https://yourdomain.com
# Or for development:
# NEXTAUTH_URL=http://localhost:3000

# Generate a secure secret: openssl rand -base64 32
NEXTAUTH_SECRET=your-secret-key-here-generate-a-random-string

# ============================================
# Google OAuth (for seamless sign-in)
# ============================================
# Get these from: https://console.cloud.google.com/apis/credentials
GOOGLE_CLIENT_ID=your-google-client-id.apps.googleusercontent.com
GOOGLE_CLIENT_SECRET=your-google-client-secret

# ============================================
# SMTP Configuration (for email verification)
# ============================================
# Required for email verification to work
SMTP_HOST=smtp.gmail.com
SMTP_PORT=587
SMTP_USER=your-email@gmail.com
SMTP_PASS=your-app-password
SMTP_SECURE=false
SMTP_REJECT_UNAUTHORIZED=true

# Email sender address
MAIL_FROM=noreply@yourdomain.com

# ============================================
# Application Configuration
# ============================================
NODE_ENV=production
PORT=3000

# ============================================
# Optional: Local LLM (Ollama)
# ============================================
# OLLAMA_BASE_URL=http://localhost:11434
# OLLAMA_MODEL=llama3.2

# ============================================
# Payment Processing (App Builder)
# ============================================
# PayPal Configuration
# Get these from: https://developer.paypal.com/
PAYPAL_CLIENT_ID=your-paypal-client-id
PAYPAL_CLIENT_SECRET=your-paypal-client-secret
PAYPAL_MODE=sandbox
# Use 'sandbox' for testing, 'live' for production
# PAYPAL_WEBHOOK_ID=your-paypal-webhook-id

# Stripe Configuration (Alternative to PayPal)
# Get these from: https://dashboard.stripe.com/apikeys
# STRIPE_SECRET_KEY=sk_test_your-stripe-secret-key
# STRIPE_PUBLISHABLE_KEY=pk_test_your-stripe-publishable-key
# STRIPE_WEBHOOK_SECRET=whsec_your-webhook-secret

# Payment Processing Base URL (for webhooks)
# PAYMENT_WEBHOOK_URL=https://yourdomain.com/api/app-builder/payments/webhook

# ============================================
# App Builder Configuration
# ============================================
# Free credits for new users (one-time welcome bonus)
APP_BUILDER_FREE_CREDITS=100

# Free tier monthly credits (for non-commercial use)
APP_BUILDER_FREE_CREDITS_PER_MONTH=50

# Referral credits (credits awarded for each successful referral)
APP_BUILDER_REFERRAL_CREDITS=100

# Default credit pricing (can be overridden in code)
# Credit to token conversion rate
CREDIT_TO_TOKEN_RATIO=100

# ============================================
# Production Notes
# ============================================
# 1. Never commit .env.local to git
# 2. Use strong, unique passwords
# 3. Enable MongoDB authentication in production
# 4. Use HTTPS in production (NEXTAUTH_URL should be https://)
# 5. Set up proper backup strategy for MongoDB
# 6. Configure firewall rules for MongoDB port (27017) if exposed

